import 'package:flutter/material.dart';

class TemplatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Page Templates", textScaleFactor: 2.0),
    );
  }
}
