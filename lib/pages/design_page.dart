import 'package:flutter/material.dart';

class DesignPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Page Design", textScaleFactor: 2.0),
    );
  }
}
